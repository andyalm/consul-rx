@foreach (var service in Model.Services)
{
    @:@service[0].ServiceName
    foreach (var s in service)
    {
        @:@s.Address @s.ServicePort
    }

}
@foreach (var kv in Model.KVPairs)
{
    @:@kv.Key - @System.Text.Encoding.UTF8.GetString(kv.Value)
}